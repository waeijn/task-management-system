{
  "name": "task-management-system",
  "version": "1.0.0",
  "description": "Task Management System Workspace",
  "private": true,
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "restart": "docker-compose down && docker-compose up -d",
    "logs": "docker-compose logs -f"
  }
}
